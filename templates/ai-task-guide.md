# AI Task Processing Guide for Claude Code

This guide explains how to process AI tasks generated by the Hugo Book Manager CLI.

## Overview

Two AI task types are produced by this project:

| Task type | Trigger | Input file | Output file |
|---|---|---|---|
| `batchGenerateMetadata` | `init-books` | `ai-tasks/input/batch-metadata-request.json` | `ai-tasks/output/batch-metadata-response.json` |
| `migrateLeafTaxonomy`   | `migrate-topic-tiers` | `ai-tasks/input/migrate-leaf-request.json` | `ai-tasks/output/migrate-leaf-response.json` |

When the user says **"請處理 AI 任務"**, look in `ai-tasks/input/` to see which
type is pending and follow the matching section below.

## Task Type 1: Batch Metadata Generation

**Purpose:** Generate metadata AND docs structure for the queued book.
Despite the `batch` file names, the CLI writes **one book per cycle** — a queue
of N books means N rounds of request → response → rerun.

**Input file:** `ai-tasks/input/batch-metadata-request.json`

```json
{
  "taskType": "batchGenerateMetadata",
  "promptFile": "templates/prompts/book-metadata.txt",
  "books": [
    {
      "bookId": "atomic-habits",
      "chineseTitle": "原子習慣",
      "englishTitle": "Atomic Habits",
      "tableOfContents": "第一部 基礎\n第一章 習慣的複利效應\n..."
    }
  ]
}
```

**Steps:**

1. Read the prompt template from `promptFile` (current schema: 6-entry topics list, three-tier categories).
2. Load `templates/topic-taxonomy.yaml` so you can pick `leaf-Z` from the existing whitelist.
3. For each book, generate metadata + docs structure.
4. Write the combined results to `ai-tasks/output/batch-metadata-response.json`.

**Output format (current schema v2):**

```json
{
  "results": [
    {
      "bookId": "atomic-habits",
      "metadata": {
        "repoName": "atomic-habits",
        "englishTitle": "Atomic Habits",
        "chineseTitle": "原子習慣",
        "description": "Atomic Habits | James Clear | A practical framework for building good habits and breaking bad ones through tiny, identity-driven changes that compound over time.",
        "topics": [
          "hugobook", "nplus-portal", "nplus-kind-book",
          "leaf-growth", "sub-mindset", "top-personal"
        ],
        "topCategory": "personal",
        "subCategory": "mindset",
        "leafCategory": "growth"
      },
      "structure": {
        "sections": [
          {
            "folderName": "01-fundamentals",
            "title": "基礎",
            "weight": 1,
            "chapters": [
              { "folderName": "01-compound-effect", "title": "習慣的複利效應", "weight": 1 }
            ]
          }
        ]
      }
    }
  ]
}
```

The `topics` array MUST have exactly 6 entries in this order:
`[hugobook, nplus-portal, nplus-kind-book, leaf-<Z>, sub-<Y>, top-<X>]`

The three `*Category` fields are the same values as topics #4/#5/#6 but **without** the prefix.
The CLI uses them to compute the local clone path: `<top>/<sub>/<leaf>/<repoName>`.

## Task Type 2: Migrate Leaf Taxonomy

**Purpose:** For an existing book repo (already on GitHub with the legacy 2-tier
topics), pick the correct three-tier `(topCategory, subCategory, leafCategory)`
triple. The CLI then applies the `leaf-Z` topic on GitHub and moves the local
clone into the new folder layout.

See `templates/prompts/migrate-leaf.txt` for the full prompt and output format.

## One-stop init-books flow

When user says **"請處理 AI 任務"** and `batch-metadata-request.json` is present:

1. Generate the response (see Task Type 1).
2. Run the CLI to complete:

   ```bash
   ./gradlew installDist --quiet
   echo -e "yes\nyes" | ./build/install/hugo-book-manager/bin/hugo-book-manager init-books
   ```

   This will:
   - Create the GitHub repo from the template
   - Clone to `<DEFAULT_WORK_DIR>/<top>/<sub>/<leaf>/<repoName>/`
   - Update template files
   - Download cover image
   - Create docs structure
   - Commit + push, wait for `gh-pages`, enable Pages

3. Report the final result to the user.

> Duplicate detection is handled by the CLI via `templates/existing-repos.yaml`
> (refresh with `./gradlew refreshRepoIndex` before queueing). Don't pre-check
> with `gh repo view`.

## Important Notes

- Always use Traditional Chinese (繁體中文) for Chinese text.
- `chineseTitle` becomes the site title, so it must be the **official Taiwan
  edition title** copied verbatim (including any English the official title
  carries). No Traditional Chinese edition → use the English original title;
  never invent a translation. The description's first segment stays the
  English original title regardless (Chinese only for Chinese-native books).
- For `repoName` and `folderName`, use lowercase kebab-case.
- **Strip a leading "The "/"A "/"An " article** from the English title before
  slugifying. Example: `The Lean Startup` → `lean-startup`. The CLI also
  strips a leading `the-` defensively when parsing the response (see
  `AiTaskService.sanitizeRepoName`); if that safety net fires you'll see a
  `⚠ Stripping leading 'the-'` warning.
- JSON output must be valid (no markdown fences).
- Only invent NEW `leaf-Z` values when no existing whitelist entry fits.
  Never invent new `top-` or `sub-` values — those are fixed.

## Common Mistakes to Avoid

- ❌ Emitting only 5 topics (missing `leaf-Z`) — schema is now 6 entries.
- ❌ Using legacy `category: "growth-book-summary"` — replaced by three `*Category` fields.
- ❌ Inventing a new `sub-` or `top-` value not in the taxonomy.
- ❌ Wrong topic order — must be `[hugobook, nplus-portal, nplus-kind-book, leaf, sub, top]`.

## Error Recovery

If a task fails midway:
1. Check `books-queue.yaml` status field.
2. Use `./gradlew initBooks -Pid=<book-id> -Preset=true` to reset.
3. Clear AI task files: `rm ai-tasks/input/*.json ai-tasks/output/*.json`.
